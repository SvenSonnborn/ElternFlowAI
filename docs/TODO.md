# TODO

Aktive Follow-ups aus laufender Arbeit. Workflow: **CLAUDE.md → "Out-of-scope TODOs"** (anhängen wenn entdeckt, entfernen wenn erledigt).

## Calendar (V1 — siehe [ADR-008](./decision-log.md))

- **Reminder gelten für die ganze Serie, nicht pro Termin** ([features/calendar/reminders.ts](../features/calendar/reminders.ts)): Die `reminders`-Tabelle hängt an `event_id`, hat keine `occurrence_date`-Spalte. Ein Switch im Detail-Sheet eines wiederkehrenden Termins schaltet die Erinnerung damit für alle Occurrences. Pro-Occurrence-Erinnerungen bräuchten eine Schema-Erweiterung — erst sinnvoll, wenn der Bedarf real ist.
- **Ganztägig ist im Edit-Form nicht umschaltbar** ([app-sections/event/EventEditScreen.tsx](../app-sections/event/EventEditScreen.tsx)): Das Form respektiert `all_day` jetzt (Zeitfelder ausgegraut, Range wird beim Speichern auf 00:00–23:59 gesnappt), kann das Flag aber nicht ändern — nur das Create-Form hat den Switch. Ein Toggle bräuchte `all_day` in `EventChanges`, das zugleich das Per-Occurrence-Override-JSON ist; `applyOverride` in [expand.ts](../features/calendar/expand.ts) kennt den Key nicht, der Override-Vertrag müsste also mitwachsen (siehe Spalten-Comment in [20260529091933_calendar.sql](../supabase/migrations/20260529091933_calendar.sql)).
- **Reminder-Rows werden noch nicht zugestellt** ([features/calendar/reminders.ts](../features/calendar/reminders.ts)): Der Client schreibt/löscht `reminders`-Zeilen, aber `sent_at` stempelt niemand — der pg_cron + Edge-Function-Worker fehlt (siehe Tabellen-Comment in [supabase/migrations/20260529092933_reminders.sql](../supabase/migrations/20260529092933_reminders.sql)). Kommt mit der Expo-Notifications-Iteration.
- **Kollisionsprüfung reicht nur so weit wie das geladene Monatsfenster** ([app-sections/event/EventCreateScreen.tsx](../app-sections/event/EventCreateScreen.tsx) — `conflicts`): Die Prüfung deckt seit der Multi-Day-Iteration die volle Terminspanne ab, arbeitet aber auf `useFamilyEvents(startAt)` — also dem Monat um den Starttag ±7 Tage. Eine Spanne, die darüber hinausreicht, wird nur im geladenen Ausschnitt geprüft. Saubere Lösung ist ein am Formular-Range ausgerichtetes Query-Fenster; das ist eine Änderung an der Query-Ebene und war bewusst nicht Teil der Iteration.
- **Balken-Budget im Monatsraster ist zwei pro Tag** ([features/calendar/spans.ts](../features/calendar/spans.ts) — `MAX_BARS`): Ab dem dritten mehrtägigen Termin an einem Tag fällt die Spanne in die Dot-Reihe zurück und verliert damit ihre visuelle Verbindung über die Tage. Mehr Balken brauchen mehr Zellhöhe im Raster — erst relevant, wenn drei parallele Spannen real vorkommen.
- **Aus dem Fenster verschobene `modified`-Exceptions verschwinden** ([features/calendar/expand.ts](../features/calendar/expand.ts) — `expandRecurrence`): Die Kandidaten einer Serie kommen ausschließlich aus `rule.between(searchStart, rangeEnd)`. Verschiebt eine `modified`-Exception ein Vorkommen per `override.start_at` in einen Monat, in dem das _ursprüngliche_ `occurrence_date` nicht liegt, wird der Kandidat nie erzeugt — der Termin ist an beiden Daten unsichtbar. Über `EventEditScreen` mit Scope „Nur diesen“ ist das erreichbar. Der Fehler ist älter als die Multi-Day-Iteration (die das Suchfenster nur verbreitert, nie verengt hat) und gehört zum Override-Modell, nicht zur Spannen-Darstellung: die Kandidatenmenge müsste die `event_exceptions` mit einbeziehen und gegen die regulär expandierten Vorkommen dedupliziert werden. Eigene Iteration inklusive Test für genau diesen Fall. (CodeRabbit-Finding aus der Multi-Day-Iteration.)
- **Kalender-Legende zeigt eine Kategorie ohne Datenbasis** ([KalenderScreen](<../app-sections/(tabs)/kalender/KalenderScreen.tsx>) — `LEGEND`): Die Liste ist hartcodiert auf `arzt`/`schule`/`sport`/`ha`/`family`/`meal`, die `event_types`-Tabelle führt aber `schule`/`training`/`arzt`/`family`/`meal`. `training` ist unkritisch — [palette.ts](../features/calendar/palette.ts) mappt Slug, Farbe, Icon und Label bewusst auf `sport`. Übrig bleibt `ha`: die Legende wirbt mit einem Swatch „HA“/„Tasks“, zu dem es keinen `event_types`-Datensatz gibt, ein Termin dieser Farbe kann also nie erscheinen. Entweder den Eintrag entfernen oder die Legende aus `useEventTypes()` speisen, damit sie nicht erneut von der Tabelle wegdriften kann. Die Legende ist Teil der Pattern-Beschreibung, Änderung daher mit dem Designer abstimmen ([patterns/calendar.md](../patterns/calendar.md)).
- **Voice-Add-Flow** ist eigene Iteration (an STT/LLM-Provider gekoppelt). Pattern-doc-Eintrag bleibt offen, getypter Form-Flow ist mit V1 jetzt da.
- **Recurrence-Editor bleibt für nicht-darstellbare Regeln verborgen** ([features/calendar/createMutation.ts](../features/calendar/createMutation.ts) — `rruleToRecurrence`): Serien mit `yearly`, `interval > 1` oder beliebigem Weekday-Set liegen außerhalb der fünf V1-Optionen; das Edit-Form zeigt dann kein Radio, statt die Regel beim Speichern auf die nächstgelegene Option zu runden (siehe ADR-008, Decision 3). Erst relevant, wenn Regeln aus einem iCal-Import oder einem echten RRULE-Editor stammen — beides existiert noch nicht.
- **Wiederholung lässt sich nicht „ab hier“ ändern** ([app-sections/event/EventEditScreen.tsx](../app-sections/event/EventEditScreen.tsx)): Eine Rule-Änderung erzwingt Scope „alle" und löscht die `event_exceptions` der Serie (ADR-008, Decision 2). Ein Forward-Split mit _neuer_ Regel (Kopf behält die alte, Tail bekommt die neue) wäre machbar — `insertSplitEvent` müsste dafür die neue Regel statt der des Masters übernehmen —, ist aber ohne Nutzersignal Spekulation.
- **Neue Kalender-Keys fehlen in `docs/COPY.md`** ([features/i18n/locales/de.json](../features/i18n/locales/de.json) + [en.json](../features/i18n/locales/en.json)): `cal.edit.fieldStartDate`/`fieldEndDate`/`error.invalidDateRange`/`recurrenceAppliesToAll`, `cal.create.fieldRecurrenceCount`/`recurrenceCountUnlimited`/`error.invalidCount`, `cal.detail.reminderError` sowie `cal.span.allDay`/`from`/`until`/`through`/`dayOf` (Beschriftung mehrtägiger Termine in der Tagesliste) und `cal.a11y.event`/`eventSpan` (Screenreader-Label der Terminzeile). Entfallen sind `cal.edit.fieldDate` und `cal.edit.error.multiDay`. Vom Designer in der Copy-Deck-Tabelle nachtragen (gleiche Baustelle wie `set.footer`), damit COPY.md Source of Truth bleibt. Ebenso kennt [patterns/calendar.md](../patterns/calendar.md) das „Endet nach … Terminen“-Feld noch nicht — die Recurrence-Sektion beschreibt nur die 5-Optionen-Radiogroup; und die V1-Beschreibung nennt nur „bis zu 3 Farbdots“ pro Tag, nicht den Spannen-Balken für mehrtägige Termine.

## Auth / Onboarding

- **Onboarding-Resume nach Abbruch** (Approach C — Auth-Spec): User mit `current_family_id() !== null` aber abgebrochenem Onboarding (kein Partner eingeladen, kein Kind angelegt) landet aktuell direkt auf Dashboard, statt Step 3/4 wieder aufzunehmen. Aktuell durch Empty-State auf [patterns/dashboard-empty.md](../patterns/dashboard-empty.md) abgefangen — V2 sollte eine "Onboarding fortsetzen"-CTA auf dem Dashboard zeigen (sobald `children`-Count == 0 oder `family_invitations`-Count == 0), die per Deep-Link wieder in den passenden Step springt. Dezimiert die Re-Entry-Friction.
- **Invite-Link von bereits-Mitglied geöffnet** ([features/auth/deepLinkHandler.ts](../features/auth/deepLinkHandler.ts) + Accept-Screen): Tippt ein User, der schon zu einer Familie gehört, auf einen `elternflow://invite/{token}`-Link, landet er im Namen-Eingabe-Formular und bekommt erst beim Absenden den RPC-Fehler `user already belongs to a family` (23505). Stattdessen sollte der Deep-Link-Handler früh kurzschließen und eine freundliche „Du bist bereits Teil einer Familie“-Meldung zeigen, statt das Accept-Formular anzubieten. (Aufgefallen beim Solo-Test des Invite-Flows.)

## Familie / Child-Profile (Live-Daten V1)

- **Voice-Add im Child-Profile** ([app-sections/child-profile/ChildProfileScreen.tsx](../app-sections/child-profile/ChildProfileScreen.tsx) — `child.voiceAdd`-Button) bleibt disabled-Stub, an STT/LLM-Provider gekoppelt.
- **„+ Andere“-Custom-Allergen** ist nicht umgesetzt — der Allergie-Picker zeigt nur die festen `ALLERGY_KEYS`. Freitext-Allergene erfordern eigene Eingabe + Keys-Strategie (sonst Locale-Coupling wie bei `SAMPLE_SEEDS`).
- **Likes/Dislikes ohne AI-Vorschläge** ([ChildProfileScreen.tsx](../app-sections/child-profile/ChildProfileScreen.tsx) — `TagEditor`). Aktuell reiner Freitext-Tag-Editor; das Pattern ([patterns/child-profile.md](../patterns/child-profile.md)) sieht „suggestions from AI corpus“ vor — kommt mit dem LLM-Layer.
- **Farb-Picker a11y-Labels** ([ChildProfileScreen.tsx](../app-sections/child-profile/ChildProfileScreen.tsx) — `AVATAR_COLORS`-Buttons): alle Farb-Buttons teilen sich `accessibilityLabel={t("child.colorOption")}`, Screenreader können sie nicht unterscheiden. Distinkte Labels bräuchten lokalisierte Farbnamen → neue Keys in der Designer-eigenen `docs/COPY.md`, daher an Designer-Freigabe gekoppelt (CodeRabbit-Finding).
- **Parent-Subtitle/-Edit im Familie-Tab** ([app-sections/(tabs)/familie/FamilieScreen.tsx](<../app-sections/(tabs)/familie/FamilieScreen.tsx>)): `parents`-Row hat keine `email`-Spalte (E-Mail liegt in `auth.users`), darum zeigt die Parent-Card nur den Namen. Parent-Profil bearbeiten ist noch nicht verdrahtet. (Der „Partner einladen“-Button ist seit der Invite-Iteration live — teilt sich den `useInvitePartner`-Flow mit Onboarding-Step 3.)

## Einstellungen (Phase 1)

- **Coming-soon-Rows ohne Ziel** ([app-sections/settings/SettingsScreen.tsx](../app-sections/settings/SettingsScreen.tsx) — `soon`-Flag): Sprachassistent-Settings (Persona/Sprache/Wake-Word laut [patterns/settings-voice.md](../patterns/settings-voice.md)), Datenschutz, Verknüpfte Apps und Hilfe & Support sind als „Bald verfügbar“ markiert, weil es keine Zielscreens gibt. Jeder Punkt ist eine eigene Iteration; Sprachassistent hängt zusätzlich am STT/LLM-Provider.
- **`set.footer` fehlt in `docs/COPY.md`** ([features/i18n/locales/de.json](../features/i18n/locales/de.json) + [en.json](../features/i18n/locales/en.json)): Die Settings-Footer-Zeile (`"Eltern Flow · v{{version}} · Made in Berlin"`, in DE und EN identisch) liegt jetzt als Key in den Catalogs, ist aber noch nicht in der designer-eigenen Copy-Deck-Tabelle erfasst. Vom Designer nachtragen, damit COPY.md die Source of Truth bleibt.
- **Logout-Confirm hat keinen Body-Text** ([SettingsScreen.tsx](../app-sections/settings/SettingsScreen.tsx) — `confirmSignOut`): `Alert.alert(t("set.logout"), undefined, …)` zeigt auf Android einen Dialog mit sichtbar leerem Textbereich (auf iOS unauffällig). Ein Body bräuchte einen neuen Key in der designer-eigenen [docs/COPY.md](./COPY.md) (z. B. `set.logoutConfirm`), daher an Designer-Freigabe gekoppelt.
- **Account-Detail-Route hinter der Profil-Card** ([SettingsScreen.tsx](../app-sections/settings/SettingsScreen.tsx)): Das Pattern sieht „Tap → account detail route“ vor. Die Card zeigt jetzt Live-Name/-E-Mail, ist aber nicht pressbar — Route existiert nicht. Sinnvoll gemeinsam mit „Parent-Profil bearbeiten“ aus dem Familie-Tab.
- **Mitteilungen zeigen keinen echten Status** ([SettingsScreen.tsx](../app-sections/settings/SettingsScreen.tsx) — `Linking.openSettings()`): Die Row deep-linkt in die OS-Einstellungen, kennt den Berechtigungsstatus aber nicht (deshalb kein „An“/„Aus“-Wert mehr). Mit der Expo-Notifications-Iteration den echten Permission-State lesen und anzeigen. Auf Web ist `openSettings` nicht implementiert → Row dort als „Bald verfügbar“.
- **Build-Nummer fehlt** ([app.json](../app.json)): weder `ios.buildNumber` noch `android.versionCode` gesetzt, daher zeigt der Settings-Footer nur `v{semver}` aus `expo-constants`. Beim Release-/EAS-Setup nachziehen und die Build-Nummer im Footer ergänzen.
- **`docs/architecture.md` „What's not here yet“ ist veraltet** ([docs/architecture.md](./architecture.md), Zeile ~67): behauptet „no Supabase … no auth, no onboarding, no settings screen“ — alles inzwischen vorhanden. Eigener Docs-Pass, nicht Teil dieser Iteration.

## Aufgaben / Tasks (Daten-Layer V1)

- **Erledigte Tasks sind nur 7 Tage weit sichtbar** ([features/tasks/queries.ts](../features/tasks/queries.ts) — `DONE_WINDOW_DAYS`): Das Fenster reicht für „Erledigt heute“ und die Wochenquote. Ein Verlauf/Archiv-Screen bräuchte eine eigene, paginierte Query — das ganze Archiv in die Live-Query zu ziehen würde bei jedem Screen-Öffnen mitwachsen.
- **`useCreateTask` ist nicht optimistisch** ([features/tasks/mutations.ts](../features/tasks/mutations.ts)): Toggle, Update und Delete patchen den Cache sofort, Create invalidiert nur. Eine optimistische neue Zeile bräuchte eine erfundene `id` **und** die gejointe `task_types`-Zeile, und ein Rollback ließe genau die Zeile wieder verschwinden, die der Nutzer eben angelegt hat. Erst relevant, wenn ein Screen die Latenz beim Anlegen spürbar macht.
- **Task-Zeilen haben keine Erinnerungs-Aktion mehr** ([app-sections/(tabs)/aufgaben/TaskRow.tsx](<../app-sections/(tabs)/aufgaben/TaskRow.tsx>)): Das Glocken-Icon der alten Mock-Zeile ist entfallen, weil es dekorativ war. Die `reminders`-Tabelle kennt `task_id`, aber es gibt keinen Zusteller — kommt mit der Expo-Notifications-Iteration, zusammen mit den Event-Remindern.
- **`patterns/homework.md` beschreibt einen V1/V2-Umschalter, den es nicht gibt** ([patterns/homework.md](../patterns/homework.md), Goal-Abschnitt: „browse by child or by status, switchable"): Der Screen rendert nur V2 (nach Fälligkeit). `useTasksByChild` liegt fertig im Layer und bedient V1 — es fehlt der zweite Renderpfad plus Segmented-Control und dessen Copy. Mit dem Designer abstimmen, bevor das gebaut wird.
- **`hw.due` zeigt ein nacktes Datum, der Mock zeigte relative Wörter** ([app-sections/(tabs)/aufgaben/TaskRow.tsx](<../app-sections/(tabs)/aufgaben/TaskRow.tsx>)): Aktuell steht dort „Fällig: 12. Aug“ / „Due: Aug 12“. Der gelöschte Mock (`features/sample-data/homework.ts`, siehe Git-History) führte stattdessen `"heute"`, `"morgen"`, `"Freitag"`, `"21. Mai"` — also kontextabhängig: relatives Wort für nahe Termine, Wochentag innerhalb der Woche, Datum darüber hinaus. Das ist die bessere Lösung, weil ein festes Muster unter der Überschrift „Heute fällig“ redundant und unter „Demnächst“ zu knapp ist. Braucht neue Keys (`hw.dueRelative.today`/`tomorrow`) in der designer-eigenen [docs/COPY.md](./COPY.md), die zu `hw.due` bislang überhaupt keinen Eintrag führt. (CodeRabbit-Finding aus der Screen-Iteration, dort mit einem _festen_ Muster vorgeschlagen — bewusst anders gelöst.)
- **Neue Aufgaben-Keys fehlen in `docs/COPY.md`** ([features/i18n/locales/de.json](../features/i18n/locales/de.json) + [en.json](../features/i18n/locales/en.json)): Neu sind `hw.loadError`, `hw.empty.title` und `hw.empty.sub`. **Geändert** ist `hw.sub`: der Wochentag stand als Literal im Katalog („Mittwoch · …") und wird jetzt als `{{weekday}}` interpoliert — mit echten Daten hätte dort dauerhaft Mittwoch gestanden.
- **`hw.error.*` fehlen in `docs/COPY.md`** ([features/i18n/locales/de.json](../features/i18n/locales/de.json) + [en.json](../features/i18n/locales/en.json)): `hw.error.notAuthenticated`/`staleReference`/`network`/`generic` liegen als Keys in den Catalogs, sind aber noch nicht in der designer-eigenen Copy-Deck-Tabelle erfasst. Vom Designer nachtragen, damit COPY.md Source of Truth bleibt (gleiche Baustelle wie `set.footer` und die Kalender-Keys).
- **Neue Formular-Keys fehlen in `docs/COPY.md`** ([features/i18n/locales/de.json](../features/i18n/locales/de.json) + [en.json](../features/i18n/locales/en.json)): `hw.add`, `hw.notFound`, `hw.create.*`, `hw.edit.*`, `hw.form.*`, `hw.delete.*`, `hw.type.*` sowie `hw.error.titleRequired`/`typeRequired`/`dateRequired`. Vom Designer in der Copy-Deck-Tabelle nachtragen (gleiche Baustelle wie die Kalender- und Settings-Keys).
- **`patterns/homework.md` kennt kein Anlege-Formular** ([patterns/homework.md](../patterns/homework.md)): Der Pattern-Doc beschreibt nur die drei Listen-Varianten und den Voice-Add-Flow. Die beiden Formular-Screens folgen deshalb dem Kalender-Formularmuster (Sheet, Pflichtfelder oben, Abbrechen in der Kopfzeile). Mit dem Designer abstimmen, damit der Pattern-Doc die Anatomie mitführt.
- **Kein Einstieg ins Anlegen vom Dashboard** ([app-sections/(tabs)/dashboard/DashboardScreen.tsx](<../app-sections/(tabs)/dashboard/DashboardScreen.tsx>)): Aufgaben lassen sich nur aus dem Aufgaben-Tab heraus anlegen. Ein Quick-Add auf dem Dashboard wäre naheliegend, ist aber eine Frage an das Dashboard-Pattern, nicht an diese Iteration.

## Dashboard

- **Render-Schleife beim Login-Übergang** ([app-sections/(tabs)/dashboard/DashboardScreen.tsx](<../app-sections/(tabs)/dashboard/DashboardScreen.tsx>)): Direkt nach erfolgreichem Login wirft die Dashboard-Route auf Web einmalig `Maximum update depth exceeded`. Der Stack führt über `forceStoreRerender` → `commitHookLayoutEffects`, also ein `useSyncExternalStore`-Abonnement, das in einem Layout-Effekt eine Zustandsänderung auslöst (Session-Store oder Navigation-State). Beim direkten Navigieren auf `/` tritt der Fehler **nicht** auf, nur im Übergang von `/login`. Reproduziert auf `main` wie auf Feature-Branches, also unabhängig von der Tasks-Reihe; der Screen rendert trotzdem. Aufgefallen bei der Web-Sichtprüfung des AufgabenScreen-Rewires. (Verwandt mit dem gleichnamigen Fehler im Web-Zweig von `DateTimePickerSheet`, aber anderer Auslöser.)

## Weitere Out-of-Scope-Items

- **`@expo/vector-icons` → scoped `@react-native-vector-icons/*`** ([app-sections/shared/Icon.tsx](../app-sections/shared/Icon.tsx) — nutzt `@expo/vector-icons/Feather` + `/MaterialCommunityIcons`). SDK 56 deprecatet das Bundle-Paket zugunsten von `@react-native-vector-icons/*` (ein Paket pro Icon-Set) + Codemod `npx @react-native-vector-icons/codemod`. `@expo/vector-icons` ist seit SDK 56 kein Sub-Dependency von `expo` mehr und wurde für diesen Bump explizit als Dependency ergänzt (`package.json`) — funktioniert unverändert, Migration auf die scoped Pakete ist ein separater Schritt (Icon-Namen/Imports ändern sich), kein Breaking Change dieses Bumps.
- **Realtime-Subscription** auf `events` / `event_exceptions` für Multi-User-Sync.
- **Optimistic UI** in den Calendar-Mutations (aktuell invalidate-and-refetch).
- **Toast-Component** statt `Alert.alert` für transiente Hinweise (Edit-Save-Done, Delete-Done).
- **Undo nach Delete** (Snackbar mit Re-Insert-Logic).
- **Conflict-Detection** beim Anlegen/Editieren (Pattern erwähnt es, gekoppelt an Add-Flow).
- **Sample-Daten-Strings via i18n** ([features/calendar/sample.ts](features/calendar/sample.ts) — `SAMPLE_SEEDS`). Title/Location sind aktuell DE-Literals. ~26 neue Catalog-Keys nötig (13 Events × 2 Sprachen); lohnt sich erst wenn Sample-Daten länger leben als die Auth-Iteration.
- **gustar.io Worker + Stripe + Expo Notifications** — eigene Iterationen.
- **CI: Actions auf Commit-SHA pinnen** ([.github/workflows/](../.github/workflows/)) — aktuell auf Major-Version-Tags (`@v4`/`@v5`); SHA-Pinning härtet gegen Supply-Chain-Angriffe (kompromittierte Tag-Re-Points). Sinnvoll zusammen mit dem nächsten Punkt.
- **`.github/dependabot.yml`** — automatische Updates für GitHub-Actions-Versionen und Bun/npm-Dependencies. Hält gepinnte Actions/Deps aktuell, ohne manuelles Nachziehen.
- **Branch-Protection-Rule „Status-Checks required" auf `main`** — die CI-Jobs (`quality`, `build`, `dependency-review`) und den Labeler-Job als Pflicht-Checks vor Merge setzen. Nicht als Datei versionierbar (GitHub-Repo-Settings), daher manuell in den Repo-Einstellungen.
- **Type-aware Lint in CI ohne Expo-Typen** — Expos `.expo/types` (Typed-Routes-`Href`-Union) sind git-ignored und werden in CI nicht erzeugt (`expo export` generiert sie **nicht**, nur `expo start`). Dadurch laufen type-aware Regeln wie `no-unnecessary-type-assertion` in CI ohne die strikte Route-Typisierung. Aktuell **keine** Divergenz (der einzige `as never`-Route-Cast in `RegisterScreen` war überflüssig und wurde entfernt). Falls künftig ein legitimer typisierter Cast nötig wird, der lokal/CI divergiert: leichtgewichtigen Typegen-Step finden (z. B. `expo start` headless mit Timeout) oder die generierten Typen committen — bislang nicht nötig, daher bewusst nicht gebaut.
- **Tailwind v3 → v4** ([tailwind.config.js](../tailwind.config.js) + [global.css](../global.css)/NativeWind-Preset) — beim SDK-54→57-Update (ADR-007) bewusst auf `tailwindcss@^3.4` belassen. v4 ist ein eigener Migrationsschritt (neue CSS-first-Config `@theme`, geändertes Content-Scanning, NativeWind-v4-Kompatibilität prüfen), kein Teil des Dependency-Bumps.
- **Prebuilt React Native reaktivieren** ([mise.toml](../mise.toml) — `RCT_USE_PREBUILT_RNCORE=0`) — seit SDK 55 baut iOS RN aus Source, weil CocoaPods ein stale, versions-mismatchtes `React-Core-prebuilt`-Binary zog (ADR-007). Sobald ein späteres SDK das prebuilt RN-Core stabil ausliefert, die Zeile aus `mise.toml [env]` entfernen (bzw. auf `1`) für schnellere iOS-Builds — vorher mit einem sauberen `prebuild --clean` gegenprüfen.
