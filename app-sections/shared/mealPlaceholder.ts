/**
 * Der Platzhalter für ein Rezept ohne `image_url`.
 *
 * `recipes` führt keine Emoji-Spalte — anders als die früheren Sample-Daten,
 * wo je Gericht eines hinterlegt war. Ein aus dem Titel geratenes Emoji wäre
 * schlechter als ein neutrales.
 *
 * Liegt hier, weil ihn inzwischen drei Oberflächen zeigen (Rezept-Detail,
 * Wochenraster, Meal-Hero des Dashboards) und ein vierter Ort ihn erneut
 * erfunden hätte.
 */
export const MEAL_PLACEHOLDER_EMOJI = "🍽";
