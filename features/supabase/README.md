# Supabase integration

Placeholder. Future work:

- Supabase client (URL + anon key via `expo-constants` extras)
- Auth helpers (sign-in, sign-up, OAuth)
- Realtime subscriptions for shared family data
- Database types generated from the schema
- All sensitive tables (children, allergies) must enforce RLS
